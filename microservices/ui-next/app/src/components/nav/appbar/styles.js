export const Styles = theme => ({
    appBar: {
        zIndex: theme.zIndex.drawer + 1
    },
    headerNav: {
        display: "flex",
        flexGrow: 1,
        justifyContent: "flex-end"
    }
});

export default Styles;
